{
  "name": "openssl-ssh-key-validator",
  "version": "1.0.0",
  "description": "A package to validate and verify private and public OpenSSL SSH Keys",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "ioalexander",
  "repository": "https://github.com/ioalexander/openssl-ssh-key-validator",
  "keywords": [
    "private-key",
    "validation",
    "ssl",
    "ssh",
    "typescript",
    "pem"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepublishOnly": "yarn build",
    "dev": "ts-node-dev src/index.ts",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write src/**/*.ts"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node-forge": "^1.3.13",
    "eslint": "^9.32.0",
    "jest": "^30.0.5",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "node-forge": "^1.3.1"
  }
}
